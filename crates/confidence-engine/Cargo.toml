[package]
name = "confidence-engine"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Deterministic confidence and uncertainty scoring, separate from fusion"

[dependencies]
shared-types.workspace = true
