//! Integration test crate. The library is intentionally empty; the end-to-end
//! tests live in `tests/`. Keeping them in a dedicated workspace member matches
//! the repository layout (`/tests/integration`) and exercises the crates exactly
//! as an external consumer would.
